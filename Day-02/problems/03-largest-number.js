// TODO: Write a function that return the largest number in an array

function findLargest(arr) {
  let largest = arr[0];
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] > number) {
      largest = arr[i];
    }
  }
  return largest;
}

let arr = [5, 0, 7, 10, 8, 17, 1];

const result = findLargest(arr);
console.log(result);
